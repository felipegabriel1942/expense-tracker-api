spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/expensetracker?serverTimezone=UTC
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      hibernate:
        type: trace

#server:
#  port: 8081